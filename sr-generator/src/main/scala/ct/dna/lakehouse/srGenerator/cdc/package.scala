package ct.dna.lakehouse.srGenerator

/** Reserved sub-package for change-data-capture-related AST builders / inputs (e.g. `ChangeKey`, `ChangeOnDateKey`). Currently a placeholder. */
package object cdc
