# Excel Catalog Demo Configuration

demo {
  # Test data path - relative to resources directory
  test-data {
    # Single test data path with proper Date types
    path = "test-data-typed"
  }
  
  # Spark configuration
  spark {
    master = "local[*]"
    ui.enabled = false
    sql.adaptive.enabled = false
    log.level = "ERROR"
  }
}