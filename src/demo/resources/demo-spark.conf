spark {
  master = "local[*]"
  ui.enabled = false
  sql.adaptive.enabled = false
  
  # Unified catalog configuration
  sql.catalog.unified = "ct.dna.catalog.unified.UnifiedTableCatalog"
  
  # Log level for demos
  log.level = "ERROR"
}